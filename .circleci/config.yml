version: 2.1

orbs:
  hugo: circleci/hugo@1.3.0
  aws-s3: circleci/aws-s3@3.1.0

workflows:
  sample:
    jobs:
      - hugo/build:
          version: "0.48"
          html-proofer: false
          post-steps:
            - run: echo "downgraded aws-s3 orb is good?"
            - aws-s3/copy:
                from: public/
                to: s3://sample-app-vue
                arguments: --recursive
